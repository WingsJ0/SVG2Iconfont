#!/usr/bin/env node

let argv = require('yargs').argv
let { name, input, output, format, prefix } = argv

let main = require('../dist/index.js').default
main({
  name,
  input,
  output,
  format: format && format.split(',') || [],
  prefix
})

